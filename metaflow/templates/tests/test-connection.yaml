apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metaflow.fullname" . }}-test-connection"
  labels:
    {{- include "metaflow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metaflow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
